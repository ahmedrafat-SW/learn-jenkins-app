pipeline {
    agent any

    stages {
        stage ('scm pipeline') {
            steps {
               sh '''
                    echo "run pipeline code from scm"
                    ls -la
               '''
            }
        }
    }
}
